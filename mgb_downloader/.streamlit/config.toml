# 팀원이 처음 실행할 때 걸리지 않도록 하는 설정.
#
# headless = true 로 두면 Streamlit이 첫 실행 시 물어보는 이메일 입력 프롬프트를
# 건너뛴다. (그 프롬프트에서 입력을 기다리느라 앱이 안 뜨는 것처럼 보인다.)
# 대신 브라우저를 자동으로 열어주지 않으므로, 실행 스크립트가 대신 열어 준다.
[server]
headless = true

[browser]
gatherUsageStats = false

[theme]
base = "light"
